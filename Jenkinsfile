pipeline {
    agent any
    stages {
        stage('Print Workspace') {
            steps {
                script {
                    echo "Workspace: ${env.WORKSPACE}"
                }
            }
        }
    }
}
